import { cleanup } from "@testing-library/react";

describe("LinkContact", () => {
  afterEach(() => {
    jest.clearAllMocks();
    cleanup();
  });

  test.todo("should navigate to \"Create Contact\" page");
});
